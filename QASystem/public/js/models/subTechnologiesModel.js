/*
	Model for subtechnologies we can implement default values here, 
	for sake of simplicity it is left empty for now. 
*/
define(function(require) {
  var subtechnologies = Backbone.Model.extend({
  });
    return subtechnologies;
});

