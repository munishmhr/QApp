/*
	Model for review questions we can implement default values here, 
	for sake of simplicity it is left empty for now. 
*/
define(function(require) {
  var reviewQues = Backbone.Model.extend({
  	
  });
    return reviewQues;
});
